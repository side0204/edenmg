-- =====================================================================
-- edenMG  지장이설 — 실사정보 시설 종류 추가
-- Migration 0064 — relocation_closure_type ADD VALUE '실사정보'
--
-- 실행 방법
--   Supabase Dashboard → SQL Editor → New query → 전체 복사·붙여넣기 → Run
--
-- 멱등 (여러 번 실행 가능).
--
-- 배경 (2026-05-24 owner 요청)
--   실사 도구바에 「실사정보입력」 버튼 추가 — 지도/도면 원하는 위치에 좌클릭
--   으로 실사 마커 시설을 즉시 배치. 시설 인프라(드래그·정보 패널·실사 캡처
--   첨부)를 그대로 재사용하기 위해 closure_type enum 에 신규 값 추가.
--
-- 본 마이그 내용:
--   - relocation_closure_type 에 '실사정보' enum value 추가
--
-- 카테고리는 코드(CLOSURE_TYPE_CATEGORY)에서 '설치장소' 로 매핑.
-- 도형은 큰 빨간 원 + 흰 'i' + 펄스 후광 (FacilityShape).
-- 이름 자동 「실사{seq_no}」, 클릭 즉시 배치 (모달 X).
-- 정보 패널은 실사정보일 때 첨부 사진 + 비고만 표시 (다른 시설 필드 숨김).
-- =====================================================================

alter type public.relocation_closure_type add value if not exists '실사정보';

-- =====================================================================
-- 마이그 0064 완료.
-- =====================================================================
