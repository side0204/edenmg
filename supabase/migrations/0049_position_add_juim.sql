-- =====================================================================
-- edenMG  직원 직급(position) enum 에 「주임」 추가
-- Migration 0049 — employee_position ADD VALUE '주임'
--
-- 실행 방법
--   Supabase Dashboard → SQL Editor → New query → 전체 복사·붙여넣기 → Run
--
-- 멱등(여러 번 실행 가능). 0001~0048 이 먼저 실행돼 있어야 한다.
--
-- 본 마이그 내용:
--   - employee_position enum 에 '주임' 추가 (대리와 사원 사이).
-- =====================================================================

alter type public.employee_position add value if not exists '주임' before '사원';

-- =====================================================================
-- 마이그 0049 완료.
-- =====================================================================
