-- =====================================================================
-- edenMG  지장이설 — 시설 노란색 마크 내용
-- Migration 0051 — relocation_facilities.mark_note
--
-- 실행 방법
--   Supabase Dashboard → SQL Editor → New query → 전체 복사·붙여넣기 → Run
--
-- 멱등(여러 번 실행 가능). 0001~0050 이 먼저 실행돼 있어야 한다.
--
-- 본 마이그 내용:
--   - relocation_facilities 에 mark_note 컬럼 추가.
--     노란색 마크(is_marked) 체크 시 정보 패널에서 입력하는 내용.
-- =====================================================================

alter table public.relocation_facilities
  add column if not exists mark_note text;

-- =====================================================================
-- 마이그 0051 완료.
-- =====================================================================
