-- =====================================================================
-- edenMG  M3 작업관리 — 작업 ID 컬럼 추가
-- Migration 0009 — works.order_id (청약·지장이설 작업의 발주처 오더 ID, 선택)
--
-- 실행 방법
--   Supabase Dashboard → SQL Editor → New query → 전체 복사·붙여넣기 → Run
--
-- 멱등(여러 번 실행 가능). 0008 이 먼저 실행돼 있어야 한다.
--
-- 정책 (owner 결정):
--   - 청약·지장이설 작업에만 입력 칸 노출 (다른 카테고리는 폼에서 숨김)
--   - 항상 선택값. 모르면 비울 수 있음
--   - 단순 자유 텍스트 (50자 제한은 server action 에서)
-- =====================================================================

alter table public.works
  add column if not exists order_id text;
