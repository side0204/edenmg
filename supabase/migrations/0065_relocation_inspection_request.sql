-- =====================================================================
-- edenMG  지장이설 — 실사정보 시설 실사요청 컬럼 추가
-- Migration 0065 — relocation_facilities.inspection_request
--
-- 실행 방법
--   Supabase Dashboard → SQL Editor → New query → 전체 복사·붙여넣기 → Run
--
-- 멱등 (여러 번 실행 가능).
--
-- 배경 (2026-05-24 owner 요청)
--   실사정보 시설 정보 패널에 「실사요청」 입력란 추가 (실사내용 비고 위에 배치).
--   설계자가 현장 작업자에게 "이 위치에서 X 를 확인해 주세요" 같은 요청을
--   미리 적어두고, 작업자가 실사 후 「실사내용(비고)」 에 결과를 기록.
--
--   기존 notes(비고) 컬럼은 실사내용 그대로 활용. inspection_request 컬럼만 신규.
--   다른 시설 종류에는 영향 없음 — 실사정보 시설에서만 UI 노출.
-- =====================================================================

alter table public.relocation_facilities
  add column if not exists inspection_request text;

-- =====================================================================
-- 마이그 0065 완료.
-- =====================================================================
