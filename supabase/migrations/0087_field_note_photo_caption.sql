-- =====================================================================
-- edenMG  현장관리 — 사진 설명(caption)
-- Migration 0087 — relocation_field_note_photos.caption
--
-- 실행 방법
--   Supabase Dashboard → SQL Editor → New query → 전체 복사·붙여넣기 → Run
--
-- 멱등(여러 번 실행 가능).
--
-- 배경
--   owner 요청 (2026-05-28): 현장관리 사진 등록 시 「어떤 사진인지 설명」을
--   입력할 수 있게 + 상단 갤러리에서 설명과 함께 모아 보기.
-- =====================================================================

alter table public.relocation_field_note_photos
  add column if not exists caption text;

-- =====================================================================
-- 마이그 0087 완료. (caption 은 nullable, 기존 사진은 NULL)
-- =====================================================================
